📑 Agenda for Week 6 day 01: Access Modifiers[Encapsulation] and Inheritance

Recap
Access Modifiers
Breakout
Inheritance

Access Modifiers:
-----------------

Access Modifiers control visibility of properties and methods in a class.

1. Public (default): Accessible everywhere

Ex: public name:string = "Sindhu"

2. Private: Accessible only inside same class, access to the property from outside the class can be
 controlled through get and set methods.

EX: private salary:number = 50000;

3. Protected: Accessible inside the class and inherited child class through extends keyword.

Ex: 
class Parent {
  protected age:number = 25;
}

class Child extends Parent {
  show() {
    console.log(this.age);//read data before modification
    this.age=30 // modify the data
     console.log(this.age) //read data after modification
  }
}

4. readonly : constant/final --> cannot change the assigned value

Ex: public readonly esalary = 20000//avail everywhere but readonly

5. static: belongs to the class and not to the object --> can be accessed without creating the 
object of the class.

Ex: private static email = "www.gmail.com" //explicit access modifier
    static email = "www.gmail.com"  //default public

//can be accessed using classname
console.log(classname.email)

Note: 1. Always use explicit modifiers that makes code more readable and maintainable
      2. Static members are not inherited, but they are accessible in the child class using the 
      parent or child Class name, not through object or this keyword.



Q] When do we use static in a TypeScript framework?

We use static when the data or behavior is:

Common for the entire framework
Does NOT depend on object state
Utility / helper / configuration
Shared across tests, pages, or services

Static for Constants & Configuration
Use case:

URLs
Timeouts

class Config {
  static BASE_URL = "https://leaftaps.com";
  static TIMEOUT = 5000;
}

ex:
Usage (no object needed)
await page.goto(Config.BASE_URL);


Single source of truth
Easy to maintain


Classroom Activity:
------------------

Create method add() as public, sub() as private, mul() as protected and try to access it outside
the class.



Inheritance:
------------

Inheritance is the process where a child class acquires properties and methods from a parent class
for code reusability.

extend is the keyword used to inherit the class properties and methods.

1. Single Inheritance :
When one child class inherits from one parent class

Parent
  ↓
Child

EX:
class Parent {}

class Child extends Parent {}


2. Multilevel Inheritance :
When a class inherits from a class that already inherits from another class.

Grandparent
     ↓
   Parent
     ↓
   Child

ex:
class Grandparent {}

class Parent extends Grandparent {}

class Child extends Parent {}


3. Hierarchical inheritance 
 When multiple child classes inherit from the same parent class.

       Parent
       /    \
   Child1  Child2

ex:
class Parent {}

class Child1 extends Parent {}

class Child2 extends Parent {}
 

4. Hybrid Inheritance
A combination of two or more types of inheritance, such as multilevel + hierarchical inheritance.

       Grandparent
            ↓
          Parent
          /    \
      Child1  Child2

Here, multilevel + hierarchical inheritance is combined.


5.Multiple Inheritance
When a single child class inherits from more than one parent class.

Parent1 ──┐
          ↓
        Child
          ↑
Parent2 ──┘

TypeScript does not support multiple inheritance using classes:

ex: 

class Parent1 {}
class Parent2 {}

// Not allowed
class Child extends Parent1, Parent2 {}

However, TypeScript can achieve similar behavior using interfaces:

interface Parent1 {
    method1(): void;
}

interface Parent2 {
    method2(): void;
}

class Child implements Parent1, Parent2 {

    method1() {
        console.log("Parent 1");
    }

    method2() {
        console.log("Parent 2");
    }
}

ClassRoom Activity 2:
---------------------

Parent file
Create class Browser
Create 2 methods browserType(),browserVersion()

1st Child file
Create class Chrome 
Create 1 method launchBrowser
Create object for Chrome

2nd Child file
Create class Edge
Create 1 method launchBrowser
Create object for Edge





